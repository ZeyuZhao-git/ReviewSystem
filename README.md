# Review System

`./nginx-1.18.0` is Front-end of a mobile application.

You can run it on nginx server by click `nginx.exe` on windows or `sudo nginx -c your/nginx/path/conf/nginx.conf` after appropriately change the root path in this 
config file.
